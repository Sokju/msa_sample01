apiVersion: networking.k8s.io/v1beta1 # for versions before 1.14 use extensions/v1beta1
kind: Ingress
metadata:
  name: msa-svc-ui
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: ui.msasmp01.com
    http:
      paths:
      - path: /
        backend:
          serviceName: msa-svc-ui
          servicePort: 9093
