#Config Server      
spring:
  cloud:
    config:
      uri: http://${CONFIG_SERVICE_NAME:localhost}:8888
      name: msa-turbine-server
      fail-fast: true
      #password: password
      #username: user

        