#Config Server      
spring:
  cloud:
    config:
      uri: http://${CONFIG_SERVICE_NAME:localhost}:8888
      name: msa-svc-order
      fail-fast: true
      #discovery: 
      #  service-id: msa-config-server
      #  enabled: true
